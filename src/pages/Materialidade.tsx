import { DoubleMaterialityMatrix } from '@/components/materiality/DoubleMaterialityMatrix';

export default function Materialidade() {
  return (
    <div className="space-y-6">
      <DoubleMaterialityMatrix />
    </div>
  );
}
