# Web App Workspace

Transitional workspace wrapper for the existing Vite web app.

Current source files remain in the repository root (`src/`, `public/`,
`vite.config.ts`) to keep this PR under the 100-file review cap.

Run via Turbo from repository root:

- `bun run dev:web`
- `bun run build --filter=@ws/web`
