# Native App Workspace

Expo + React Native + Expo Router + TypeScript + Bun + Uniwind.

Run from repository root:

- `bun run dev:native`

This app is intentionally iOS-first for the current migration phase.
