# Convex Backend

Initial Convex workspace for shared domain contracts.

`convex/_generated/*` files are created after running:

- `bun run dev:backend`
- or `bun --cwd packages/backend run codegen`

Web and native apps will consume generated API bindings from this package.
